<div>
<%= simple_form_for(restaurant) do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :phone_number %>
  <%= f.input :category %>
  <div class="button">
    <%= f.submit "Create", class: "btn btn-primary" %>
  </div>
<% end %>
</div>
